# Copyright 2026 Dell Inc. or its subsidiaries. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---

- name: Handle coresmd transition to coresmd-coredhcp and coresmd-coredns
  block:
    - name: Check if coresmd service exists
      ansible.builtin.shell: |
        systemctl list-unit-files | grep -q "coresmd.service"
      register: coresmd_service_check
      failed_when: false
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Check if coresmd-coredhcp service exists
      ansible.builtin.shell: |
        systemctl list-unit-files | grep -q "coresmd-coredhcp.service"
      register: coredhcp_service_check
      failed_when: false
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Stop and disable old coresmd service if it exists
      ansible.builtin.systemd:
        name: coresmd
        state: stopped
        enabled: false
      delegate_to: oim
      delegate_facts: true
      connection: ssh
      when: coresmd_service_check.rc == 0

    - name: Remove old coresmd container if it exists
      ansible.builtin.command: podman rm -f coresmd
      register: remove_coresmd
      failed_when: false
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Remove old coresmd quadlet file if it exists
      ansible.builtin.file:
        path: /etc/containers/systemd/coresmd.container
        state: absent
      delegate_to: oim
      delegate_facts: true
      connection: ssh
      when: coresmd_service_check.rc == 0

    - name: Create coresmd-coredhcp quadlet file
      ansible.builtin.template:
        src: coresmd-coredhcp.container.j2
        dest: /etc/containers/systemd/coresmd-coredhcp.container
        owner: root
        group: root
        mode: '0644'
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Create coresmd-coredns quadlet file
      ansible.builtin.template:
        src: coresmd-coredns.container.j2
        dest: /etc/containers/systemd/coresmd-coredns.container
        owner: root
        group: root
        mode: '0644'
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Reload systemd daemon
      ansible.builtin.systemd:
        daemon_reload: true
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Enable and start coresmd-coredhcp service
      ansible.builtin.systemd:
        name: coresmd-coredhcp
        state: started
        enabled: true
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Enable and start coresmd-coredns service
      ansible.builtin.systemd:
        name: coresmd-coredns
        state: started
        enabled: true
      delegate_to: oim
      delegate_facts: true
      connection: ssh

    - name: Display coresmd transition success
      ansible.builtin.debug:
        msg: "Successfully transitioned from coresmd to coresmd-coredhcp and coresmd-coredns"

  rescue:
    - name: Coresmd transition failed
      ansible.builtin.debug:
        msg: "Warning: Coresmd transition encountered issues. Manual intervention may be required."
